# This workflow will do a clean install of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: React CI/CD

on: # Events that will trigger this workflow
  push:
    branches: [ ci-cd-tutorial ]
  pull_request:
    branches: [ master ]

jobs: # Jobs that will be executed on an event triggered
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: yarn install
    - run: yarn test
    - run: yarn build
    - run: git config user.email "ronin8503@gmail.com"
    - run: git config user.name "Alberto Aragon Alvarez"
    - run: git config user.username "albertoaa"
    - run: yarn deploy
